<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ColetaAlert" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/eco_forest_green</item>
        <item name="colorOnPrimary">@color/eco_text_on_primary</item>
        <item name="colorSecondary">@color/eco_vivid_blue</item>
        <item name="colorOnSecondary">@color/eco_text_on_primary</item>

        <item name="android:statusBarColor">@color/eco_forest_green</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:colorBackground">@color/eco_background_light</item>
    </style>

    <style name="NoIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="colorSecondaryContainer">@android:color/transparent</item>
        <item name="android:width">0dp</item>
        <item name="android:height">0dp</item>
    </style>

    <style name="Theme.ColetaAlert" parent="Base.Theme.ColetaAlert" />
</resources>